{
  "name": "balanced",
  "extends": "safe",
  "description": "Premium model stays the main coordinator; routine work (exploration, implementation, review) is delegated to cheaper subagents. Adds worker/reviewer agents, delegation + doctor skills, a CLAUDE.md delegation directive, statusline, and output caps. Does NOT set a top-level model — uses whatever you launch with.",
  "assets": ["agents/worker", "agents/reviewer", "skills/delegate", "skills/doctor", "statusline"],
  "claudeMd": "delegation",
  "vars": {
    "workerModel": "sonnet",
    "reviewerModel": "sonnet"
  },
  "settings": {
    "env": {
      "BASH_MAX_OUTPUT_LENGTH": "20000",
      "MAX_MCP_OUTPUT_TOKENS": "12000"
    },
    "statusLine": {
      "type": "command",
      "command": "{{configDir}}/statusline/leanrig-statusline.sh"
    }
  }
}
